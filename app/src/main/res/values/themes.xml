<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.TaiRightWords" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.TaiRightWords.Translucent">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
    </style>
    <!-- Variant of the translucent theme with no title bar -->
    <style name="Theme.TaiRightWords.Translucent.NoTitleBar">
        <item name="android:windowNoTitle">true</item>
    </style>
    <!-- Variant of the translucent theme that has no title bar and
         fills the entire screen -->
    <style name="Theme.TaiRightWords.Translucent.NoTitleBar.Fullscreen">
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>